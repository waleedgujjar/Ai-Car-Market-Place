// tailwind.config.js
import defaultTheme from 'tailwindcss/defaultTheme'

export default {
  content: ['./src/**/*.{html,js,ts,jsx,tsx}'], // adjust paths as needed
  theme: {
    extend: {
      // optional: extend color palette if needed
    },
  },
  plugins: [],
}
